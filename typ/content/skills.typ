Data analysis, financial econometrics, high performance scientific computing, parallel computing, data visualization, causal inference, matching.

`python`, `R`, `SAS`, `stata`, `sql`, `bash`, `git`, `awk`, #context { if target() == "html" { [LaTeX] } else { [#LaTeX] } }
